# ============================================
# ENVIRONMENT VARIABLES CONFIGURATION
# ============================================
# 
# This file is a template/example only.
# NEVER commit the real .env file to git!
# 
# In Easypanel, configure these variables in:
#   Environment Variables section
# 
# You can copy this file to .env locally
# for testing, but NEVER commit .env
# 
# ============================================

# ============================================
# AWS Credentials (Required)
# ============================================
# AWS credentials for S3 bucket access
# where validator checkpoints are stored

AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here
AWS_REGION=us-east-1

# ============================================
# Database Path (Optional)
# ============================================
# Database path for validator and relayer
# Optional: defaults to /etc/data/db if not set

HYP_DB=/etc/data/db

# ============================================
# S3 Bucket Configuration (Required for Validator)
# ============================================
# S3 bucket configuration where checkpoints will be stored
# You can use the same region as AWS_REGION or a different one

# S3 Bucket Name (Required for Validator)
# Must be globally unique on AWS
HYP_CHECKPOINT_SYNCER_BUCKET=hyperlane-validator-signatures-your-name-here

# S3 Bucket Region (Optional for Validator)
# If not set, will use AWS_REGION (or us-east-1 as final fallback)
HYP_CHECKPOINT_SYNCER_REGION=us-east-1

# ============================================
# Relayer - Private Keys (Required)
# ============================================
# Private keys for the relayer in hex format
# These variables override the placeholders in JSON files

# BSC Testnet - Private key in hex format (64 hex characters after 0x)
HYP_CHAINS_BSCTESTNET_SIGNER_KEY=0x1234567890abcdef1234567890abcdef1234567890abcdef1234567890abcdef

# Solana Testnet - Private key in hex format (64 hex characters after 0x)
HYP_CHAINS_SOLANATESTNET_SIGNER_KEY=0x1234567890abcdef1234567890abcdef1234567890abcdef1234567890abcdef

# Terra Classic Testnet - Private key in hex format (64 hex characters after 0x)
HYP_CHAINS_TERRACLASSICTESTNET_SIGNER_KEY=0x1234567890abcdef1234567890abcdef1234567890abcdef1234567890abcdef

# ============================================
# Validator - Private Keys (Required)
# ============================================
# Private keys for the validator in hex format
# These variables override the placeholders in JSON files

# Validator Key - Validator private key in hex format (64 hex characters after 0x)
# This is the main validator key used to sign checkpoints
HYP_VALIDATOR_KEY=0x1234567890abcdef1234567890abcdef1234567890abcdef1234567890abcdef

# Terra Classic Testnet - Signer Key (Required for Validator)
# Private key for signing transactions on Terra Classic Testnet
# Note: This is the same variable as used in Relayer section above
# Can be the same key as HYP_VALIDATOR_KEY or different
# Already defined above in Relayer section - no need to duplicate

# ============================================
# IMPORTANT NOTES:
# ============================================
#
# 1. Private Key Format:
#    - All keys must start with "0x"
#    - Followed by exactly 64 hexadecimal characters
#    - Valid example: 0x1234567890abcdef1234567890abcdef1234567890abcdef1234567890abcdef
#
# 2. S3 Bucket:
#    - The bucket name must be globally unique on AWS
#    - Recommended: hyperlane-validator-signatures-your-name-here
#    - The region can be the same as AWS_REGION or different
#
# 3. Security:
#    - NEVER commit this file with real values to git
#    - Use Easypanel to configure environment variables
#    - Keep your private keys secure and never share them
#
# 4. How to Generate Keys:
#    - BSC: Use MetaMask or Ethereum/EVM key generation tools
#    - Solana: Use solana-keygen or other Solana tools
#    - Terra Classic: Use terrad keys add or other Cosmos tools
#
# 5. Testnet vs Mainnet:
#    - For testnet, use these variables with TESTNET suffix
#    - For mainnet, the variables would be without the TESTNET suffix
#      (e.g., HYP_CHAINS_BSC_SIGNER_KEY instead of HYP_CHAINS_BSCTESTNET_SIGNER_KEY)
#
# ============================================
