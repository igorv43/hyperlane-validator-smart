# Arquivos de ambiente com credenciais
.env
.env.local
.env.*.local

# Arquivos de configuração com chaves privadas (CRÍTICO)
# Validator configs (gerados dinamicamente, NÃO commitar)
hyperlane/validator*.json
!hyperlane/validator*.json.example

# Relayer configs 
# NOTA: relayer.testnet.json e relayer.mainnet.json são commitados,
#       mas devem SEMPRE ter chaves vazias ("key": "")
#       O docker-compose injeta as chaves em runtime

# Dados dos agentes
validator/
relayer/
validator-testnet/
relayer-testnet/

# Logs
*.log

# Docker
docker-compose.override.yml

solana-keypair.json
